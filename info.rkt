#lang info

(define collection 'multi)

(define version "0.1")

(define deps
  '("laramie-lib"
    "laramie-doc"))

(define implies
  '("laramie-lib"
    "laramie-doc"))

(define pkg-authors '("jesse@serverracket.com"))
